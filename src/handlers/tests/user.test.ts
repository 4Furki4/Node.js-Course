describe('user handler', () => {
    it('should do something when something happens', () => {
        expect(1).toEqual(1)
    })
    it('should do something when something happens', () => {
        expect(1).toEqual(2)
    })
})